# Local Pi runtime state
.atl/

# Local agent/tooling state (never commit)
.pi/
.codegraph/
skills-lock.json

# Rust
target/

# Secrets (use env.example as template)
.env
odd/
.github/

# Historical research data (never commit datasets)
research-data/raw/
research-data/cache/
research-data/processed/*.parquet
research-data/processed/*.csv
research-data/reports/*.json
research-data/reports/*.csv
research-data/reports/*.parquet
# Keep: research-data/manifest.json (small, reproducible), research/config/, research/scripts/
